# content/

Authored, shippable copy — lesson text, challenge definitions, onboarding strings.

Rules of thumb:

- Data only (TypeScript objects, JSON, MDX-free strings). No components.
- Everything here is bundled with the app, so it must stay small and static.
- Content is versioned with the app; it is not fetched at runtime.
